// Re-exported from types.ts (the canonical source) for import-path
// backwards compatibility - routes/feed.ts and others already import
// CATEGORIES from here.
export { CATEGORIES, type Category } from "./types";
